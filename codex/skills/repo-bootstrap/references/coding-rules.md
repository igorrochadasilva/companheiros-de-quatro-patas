# Coding Rules Reference

- Read the relevant repository rules before editing.
- Keep functions small and focused.
- Use descriptive names.
- Avoid hardcoded UI text when the project already centralizes messages.
- Prefer the smallest change that solves the request cleanly.
- Keep runtime application code outside `codex/`.
- Validate changes before closing the task.
